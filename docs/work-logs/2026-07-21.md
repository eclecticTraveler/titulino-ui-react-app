# Work Log — 2026-07-21

## Session goal
Build the Facilitator Know Me Dashboard tab (T1–T9) and plan multi-facilitator expansion.

---

## Completed this session

### Planning
- Clarified consent decision: existing submission consent is sufficient; facilitator (teacher of record) viewing student essays is the normal educational relationship. No extra consent column needed.
- Clarified that DB already supports multiple facilitators per course via `"Enrollment"."UserRole_Course"` (many-to-many). Adding a second facilitator = one row insert.
- Wrote plan file: `docs/plans/active/2026-07-21-facilitator-know-me-dashboard.md`
- Documented Phase 2 (multi-facilitator expansion) in the same plan: 4 tasks identified, nothing built yet.

### T1 — Warehouse (Sqitch migration)

**Migration:** `Lrn/2026/07/21_facilitator_know_me_status`

Two new functions:
- `"Lrn".get_know_me_ai_status_by_course(p_course_code_id text)` — inner; SECURITY INVOKER. JOINs KnowMeSubmission → KnowMeAiJob (LEFT) → vm_Enrollee. `DISTINCT ON (ContactInternalId, ClassNumber)` with `ORDER BY CreatedAt DESC` picks latest AI job per student per chapter.
- `"TitulinoApi_v1"."GetKnowMeAiStatusByCourse"(p_course_code_id text)` → jsonb — SECURITY DEFINER wrapper. Granted to titulino_facilitator, titulino_administrator, super_admin.

### T2 — titulino-net-api: RepositoryClient

- `SupabaseAdapter.cs` — added `GetKnowMeAiStatusByCourseAsync(courseCodeId)`: POSTs to `GetKnowMeAiStatusByCourse` with `{ p_course_code_id }`, returns raw JSON string.
- `IRepositoryClient.cs` — added `Task<string?> GetKnowMeAiStatusByCourseAsync(string courseCodeId)`.
- `RepositoryClient.cs` — added implementation delegating to `_supabaseAdapter`.

### T3 — titulino-net-api: LrnManager + DTO

- New `DTOs/KnowMeAiStatusEntry.cs` — fields: ContactInternalId, FullName, ClassNumber, Status, CompletedAt, OriginalEssays, CorrectedEssays, Feedback.
- `ILrnManager.cs` — added `Task<IEnumerable<KnowMeAiStatusEntry>> GetKnowMeAiStatusByCourseAsync(string courseCodeId)`.
- `LrnManager.cs` — added implementation: parses the jsonb array, maps each element to KnowMeAiStatusEntry using TryGetProperty pattern consistent with GetKnowMeAiResultAsync.

### T4 — titulino-net-api: LrnController endpoint

- `Endpoints/Lrn.cs` — added `GET /v1/lrn/know-me/ai-status?courseCodeId=...`
- Auth policy: `GlobalAccessAdminUserOrFacilitator`
- Returns `IEnumerable<KnowMeAiStatusEntry>` as JSON array.

### T5 — titulino-ui: TitulinoLrnNetService

- `src/services/Lrn/TitulinoLrnNetService.js` — added `getKnowMeAiStatusByCourse(token, courseCodeId)`: GET to `/know-me/ai-status`, returns `{ success, entries: [] }`.

### T6 — titulino-ui: Redux action

- `src/redux/constants/Lrn.js` — added `ON_FETCHING_KNOW_ME_AI_STATUS_BY_COURSE`.
- `src/managers/LrnManager.js` — added `getKnowMeAiStatusByCourse(courseCodeId, emailId)`: resolves token via `getCachedUserProfile`, calls service.
- `src/redux/actions/Lrn.js` — added `onFetchingKnowMeAiStatusByCourse(courseCodeId, emailId)`.

### T7 — titulino-ui: KnowMeStatusTab component

New file: `src/components/admin-components/Insights/KnowMeStatusTab.js`

- Connected component (maps `user` from `grant` Redux slice, `onFetchingKnowMeAiStatusByCourse` via bindActionCreators).
- Fetches data on mount (cancellable, guards on courseCodeId + emailId).
- Section 1: ColumnBar chart — X = chapter number label, Y = submission count.
- Section 2: Select filter by chapter number.
- Section 3: Ant Design Table with expandable rows.
  - Columns: Student name, Chapter, Status (Tag), Completed at.
  - Status tags: completed (success), pending (processing), processing (warning), failed (error), null (default).
  - Expandable: `EssayCards` sub-component renders original answer → corrected version → English feedback → native language feedback — same card layout as `KnowMeV3.js`.

### T8 — titulino-ui: FacilitatorsLandingDashboard

- Imported `faPenToSquare` (FontAwesome) and `KnowMeStatusTab`.
- Added "Know Me" tab between Trends and My Progress. Tab renders `<KnowMeStatusTab courseCodeId={courseCodeId} />`.
- i18n key: `facilitador.dashboard.knowme` (add to translation files when adding language support).

---

## Deployment order

1. `sqitch deploy` on prod DB (warehouse)
2. Build + deploy titulino-net-api
3. Release titulino-ui

Worker is untouched. titulino-communication is untouched.

---

## Next session

- Deploy and verify: Does the Know Me tab appear? Do submissions load? Does the essay expand correctly?
- Phase 2 planning: Admin UI to assign facilitator roles (P2-T1/T2), multi-course switcher (P2-T3).
- Archive plan to `docs/plans/completed/` after deploy verification.
- Add `facilitador.dashboard.knowme` i18n key to translation files if bilingual support is needed.
