# 2026-07-01 — CI/CD manual dispatch audit + titulino-docs fix

## Domain: DevOps — GitHub Actions

### Summary

Audited all Titulino CI/CD workflows for manual deploy dropdown support (`workflow_dispatch`). Fixed `titulino-docs`. Documented current state across all repos.

---

### CI/CD workflow state — all repos

| Repo | Workflow file | Auto triggers | Manual dropdown |
|------|--------------|---------------|-----------------|
| titulino-ui | `titulino-pr-action.yml` | PR → dev, push main → prod | ✅ dev / prod |
| titulino-net-api | `deploy-titulino-api.yml` | PR → dev, push master → prod | ✅ dev / prod |
| TitulinoWorkerService | `deploy-titulino-worker.yml` | PR → dev, push master → prod | ✅ dev / prod |
| titulino-communication | `deploy-titulino-missive.yml` | push master → prod | ⚠️ prod only (intentional — no dev environment planned) |
| titulino-docs | `deploy-docs.yml` | PR → dev, push main → prod | ✅ dev / prod (fixed today) |

### Change made — titulino-docs

Added `workflow_dispatch` trigger with `dev` / `prod` choice. Logic:
- Manual `dev` → `dev.docs.titulino.com` + DEV secrets
- Manual `prod` → `docs.titulino.com` + PROD secrets
- Automatic PR → dev (unchanged)
- Automatic push to main → prod (unchanged)

Committed to `main` on `titulino-docs` — live immediately.

### titulino-communication — intentionally skipped

No dev environment is planned for the Missive cron jobs. The `prod`-only dropdown is correct.

---

### Docs updated in titulino-docs repo

- `docs/titulino-worker/deployment.md` — fully rewritten with correct info:
  - Fixed `ExecStart` path (files land in `publish/` subdir — was pointing to wrong location)
  - Removed stale GCS inbox references (worker now uses DB job queue, not GCS)
  - Added manual trigger dropdown docs
  - Added OpenAI / Gemini env var table
  - Added both prod and dev systemd service file examples

---

### Next session

- **P6** — GitHub sync actions (5 source repos push `docs/**/*.md` → `titulino-docs`)
- **P7** — Docusaurus local search plugin

Plan file: `docs/plans/active/titulino-docs-site.md`
