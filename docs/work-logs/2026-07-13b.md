# Work Log — 2026-07-13 (session 2)

## Floating Action Menu — UI tasks complete (T2–T6, T12–T18)

### What was done

**T13 — `IS_FLOATING_ACTIONS_ON` flag**
- Added to `src/configs/EnvironmentConfig.js`: `false` in `commonConfig` and `prod`, `true` in `dev` and `local`

**T2+T3+T4+T5 — LOB file**
- Created `src/lob/FloatingActions.js` with:
  - Shape definitions (JSDoc comments) for `IFloatingAction` and `IFacebookMapping`
  - `resolveFacebookUrl(nativeLangId, targetLangId, mappings[], defaultUrl)` — 2-pass: most-specific (native+target) then target-only, then defaultUrl
  - `resolveVisibleActions(actions[], isAuthenticated)` — filters by `enabled` + auth visibility flags

**T6 — Tests**
- Created `src/lob/__tests__/FloatingActions.test.js` — 19 tests, all passing
- Covers: empty/undefined inputs, all mapping priority levels, all visibility filter combinations

**T12 — Redux wiring**
- `ON_FETCHING_FLOATING_ACTIONS` constant added to `src/redux/constants/Lrn.js`
- `getFloatingActionsConfig` service method added to `src/services/TitulinoNetService.js` — fetches `GET /v1/lrn/floating-actions`, returns safe default `{ version:1, actions:[], facebookMappings:[] }` on any error
- `onFetchingFloatingActions` action added to `src/redux/actions/Lrn.js`
- Reducer case added to `src/redux/reducers/Lrn.js` → `lrn.floatingActionsConfig`

**T14 — LESS**
- Created `src/assets/less/styles/custom-components/_floating-action-menu.less`
- `@bmc-button-height: 52px`, `@bmc-button-bottom: 18px` (matches `data-y_margin="18"` from widget)
- `@floating-stack-bottom: 82px` (height + margin + 12px clearance)
- Classes: `.floating-action-stack`, `.floating-action-btn`, `.floating-action-btn--facebook` (blue), `.floating-action-btn--contact` (green), `.floating-action-btn--link` (white)
- Import added to `custom-component.less`

**T15+T16+T17+T18 — Components**
- Created `src/components/layout-components/FloatingActionMenu/index.js`:
  - Fetches config on mount (when flag is on)
  - Reads `lrn.floatingActionsConfig`, `auth.token`, `lrn.baseLanguage.localeCode`, `theme.contentLanguage`
  - Renders vertical button stack above BMC widget
  - `FacebookActionButton` (T16) — resolves URL from spine mappings; opens new tab
  - `ContactActionButton` (T17 trigger) — opens modal on click
  - `LinkActionButton` (T18) — `imageUrl` + `url` + `label`
- Created `src/components/layout-components/FloatingActionMenu/ContactFormModal.js` (T17):
  - name / subject / message fields with validation
  - reCAPTCHA v3 via `useGoogleReCaptcha()` → `executeRecaptcha('contact_submit')`
  - POSTs to `${env.TITULINO_NET_API}/v1/lrn/contact` (T8, deferred)
  - Shows success/error antd `message`
- Mounted in both `src/layouts/app-layout/index.js` and `src/layouts/auth-layout/index.js`

**Localization**
- Added 14 `floating.*` keys to `en_US.json`, `es_US.json`, `pt_BR.json`

### Language ID resolution
- Native language: `lrn.baseLanguage?.localeCode` ('pt', 'en', 'es')
- Target language: `theme.contentLanguage` ('en', 'es', 'pt')

### Test results
- 305/305 tests passing
- LESS compiles clean

### What's still pending (backend — T7–T11)

| Task | Description |
|---|---|
| T7 | `GetFloatingActionsAsync()` in `GcpBucketAdapter` (titulino-net-api) + REST endpoint `GET /v1/lrn/floating-actions` |
| T8 | `POST /v1/lrn/contact` endpoint — captcha validation + `submit_contact_message` RPC |
| T9 | Sqitch migration — `ContactMessages` table, `upsert_contact_message`, `get_pending_contact_messages`, wrappers |
| T10 | `contactmessages` TitulinoMissive subcommand |
| T11 | Crontab entry on `pd-titulino-lang` |

### Next session: start with T7 (titulino-net-api — spine endpoint + contact endpoint)
